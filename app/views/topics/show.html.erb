<div class="col-md-7 blog-main">
  <h3 class="pb-3 mb-4 font-italic border-bottom">
    From the Firehose
  </h3>
  <h1><%= @topic.title  %></h1>
  <%= paginate @blogs %>
  <%= render @blogs %>
  <%= paginate @blogs %>


</div>
